---
repositories:
  - name: jenkins
    url: https://charts.jenkins.io
releases:
  - name: jenkins
    chart: jenkins/jenkins
    version: 3.2.0
    namespace: jenkins
    values:
      - "values.yml"
...
